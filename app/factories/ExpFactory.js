"use strict";

angular.module("BenApp").factory("ExpFactory", function( $document, $route, $interval){ 
    
    
    const workExp = ()=>{
        const aboutMe = ``;
        return aboutMe;
    }; 

    return {workExp};
});

